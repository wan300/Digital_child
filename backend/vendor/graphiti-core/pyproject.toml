[project]
name = "vendor-graphiti-core"
version = "0.29.1"
description = "Vendored Graphiti core package for human-memory-orchestrator"
requires-python = ">=3.10"
license = "Apache-2.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["graphiti_core"]
