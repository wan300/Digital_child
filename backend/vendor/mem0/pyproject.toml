[project]
name = "vendor-mem0ai"
version = "2.0.4"
description = "Vendored mem0 core package for human-memory-orchestrator"
requires-python = ">=3.10"
license = "Apache-2.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mem0"]
